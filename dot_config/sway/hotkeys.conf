##
## Extra hotkeys for custom stuff, that does not fit in `keybinds.conf`
## Last Update: 2023/11/02
##

# Screenshot hotkey
bindsym $meta+Shift+s exec --no-startup-id bash -c 'grim -g "$(slurp)" - | wl-copy'

# Lock screen
bindsym $meta+z exec "$HOME/bin/lock.sh locked"
