# dotfiles
My dotfiles. Always WIP.

## Setup
_This is mostly a note to self, in case I need access to these commands later_.

* Add a single file: `ln -s /path/to/file.ext /path/to/dotfiles/file.ext`
* Add a folder: `cp -al /path/to/folder /path/to/dotfiles/`

## Dotfiles

Dotfiles are used to configure various tools and programs. 

TODO: Explain all dotfiles, and why they exist.
